export const initializeApp = (user) => {
  return (dispatch, getState) => {
    // if user is logged in
    // global.axios.defaults.headers.common.Authorization = `Bearer ${JWT}`
    // dispatch(handleUploadQueue())
  }
}
